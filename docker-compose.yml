services:
  web:
    image: ${DOCKER_REGISTRY-}web
    build:
      context: .
      dockerfile: src/SchoolAccount.Web.Mvc/Dockerfile
    ports:
      - "5100:8080"

  seq:
    image: datalust/seq:2024.3
    environment:
      - ACCEPT_EULA=Y
    ports:
      - "8081:80"
