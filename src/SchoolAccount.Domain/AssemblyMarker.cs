namespace SchoolAccount.Domain;

/// <summary>
/// Marker type used to reference the SchoolAccount.Domain assembly (e.g. in architecture tests)
/// while the layer has no other public types.
/// </summary>
public static class AssemblyMarker;
